# Market Research Agent - Dependencies
# Core dependencies (minimal, no external packages required for basic functionality)

# Optional: For enhanced keyword extraction
# scikit-learn>=1.0.0
# numpy>=1.21.0

# Optional: For web scraping (if running standalone)
# requests>=2.28.0
# beautifulsoup4>=4.11.0

# Optional: For Google Trends
# pytrends>=4.9.0

# Optional: For Reddit API
# praw>=7.6.0

# Optional: For Twitter API
# tweepy>=4.12.0

# Note: This agent is designed to work with Claude Code's built-in
# WebFetch and WebSearch tools, so no external HTTP libraries are
# required when used through Claude Code.
